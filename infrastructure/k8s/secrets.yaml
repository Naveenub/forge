# ⚠️  DO NOT commit real secrets.
# Generate base64: echo -n "value" | base64
# Use sealed-secrets or external-secrets-operator in production.
apiVersion: v1
kind: Secret
metadata:
  name: forge-secrets
  namespace: forge
type: Opaque
data:
  # Replace ALL values below with real base64-encoded secrets
  anthropic-api-key: REPLACE_WITH_BASE64
  database-url: REPLACE_WITH_BASE64
  database-url-replica: REPLACE_WITH_BASE64
  jwt-secret: REPLACE_WITH_BASE64
  redis-url: REPLACE_WITH_BASE64
  postgres-password: REPLACE_WITH_BASE64
